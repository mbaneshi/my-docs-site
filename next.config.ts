// next.config.js
module.exports = {
  output: "export",
  basePath: "/my-docs-site", // Optional: if the repo is in a subfolder (e.g., https://yourusername.github.io/my-docs-site)
  trailingSlash: true, // Optional: ensures all URLs end with a trailing slash, which GitHub Pages prefers
};

// import type { NextConfig } from "next";
//
// const nextConfig: NextConfig = {
//   /* config options here */
// };
//
// export default nextConfig;
